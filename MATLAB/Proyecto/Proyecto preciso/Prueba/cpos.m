%Creación de partículas positivas
pos=input('Número de cargas positivas = ');
qp=1;
mp=1;
for f=1:pos
rxp(f)=rand*4+0.5;
ryp(f)=rand*4+0.5;
rzp(f)=rand*4+0.5;
vxp(f)=rand;
vyp(f)=rand;
vzp(f)=rand;
end
